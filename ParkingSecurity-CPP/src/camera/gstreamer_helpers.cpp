// GStreamer helper utilities used by the camera pipeline.
// Kept in a separate TU so they can be unit-tested independently.
//
// TODO: during implementation phase, add:
//   - GstCaps builders for BGR/RGB/NV12
//   - Pipeline string builders for v4l2/rtsp/file sources
//   - NvBufSurface <-> CPU conversion helpers (Jetson only)

namespace parking::camera {

// Intentionally empty. Helpers will be added as needed.

}  // namespace parking::camera
